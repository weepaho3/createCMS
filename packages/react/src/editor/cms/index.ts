'use client';

// @createcms/react/editor/cms — optional adapter between the primitive and a
// createcms client (load/save/head tracking, field sources). Reserved entry:
// the adapter hooks arrive later; nothing is exported yet on purpose.
// oxlint-disable-next-line unicorn/require-module-specifiers -- intentional empty module (reserved entry)
export {};
