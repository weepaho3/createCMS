# CMS adapter (`@createcms/react/editor/cms`)

This entry will hold the optional adapter between the editor primitive and a
createcms client — load/save, head tracking, field sources.

It is empty for now: the adapter hooks arrive with a later issue.

It stays an optional entry — consumers that bring their own data layer never
need to import it.
