---
title: Server API
description: The type-safe server-side API on cms.api, grouped by area.
---

`createCMS` returns an `api` object namespaced by collection and by system feature. You call `cms.api.<collection>.<method>` for content (e.g. `cms.api.pages.createRoot`) and `cms.api.<namespace>.<method>` for system features (e.g. `cms.api.media.uploadAssets`). Inputs and outputs are fully typed from your collection definitions.

```ts
const { roots } = await cms.api.pages.listRoots({ query: { limit: 20 } });
```

Every method follows the better-call convention: **read** methods take a `query` object, **write** methods take a `body` object. Every commit-producing mutation returns a `commit: { id, message, createdAt, createdBy }` envelope, the new branch head. The same methods are available on the [client](/docs/reference/client-api) as `client.<collection>.<method>`, with identical types.

## Permissions

Every endpoint runs an authorization check keyed by a **resource** and an **operation** (`create`, `read`, `update`, or `delete`), shown on each method as a `resource:operation` chip (for example `root:create`). [Access control](/docs/guides/permissions) maps those to your roles. The only endpoint reachable without a session is the public media gate `media.asset`; the anonymous-readable reads (`getPublishedContent`, `resolveRedirect`, and the A/B `resolveAbVariant`) still run the auth chain under the `publishedContent` resource, so plugin scope is enforced.

## Collection methods

Available on every collection as `cms.api.<collection>.<method>`.

<Cards>
  <Card title="Roots" href="/docs/reference/roots">Create, read, move, and version entries.</Card>
  <Card title="Blocks" href="/docs/reference/blocks">Read and edit the block tree of an entry.</Card>
  <Card title="Branches" href="/docs/reference/branches">Create and manage branches (drafts).</Card>
  <Card title="Merges" href="/docs/reference/merges">Diff, review, resolve conflicts, and merge.</Card>
  <Card title="Publications" href="/docs/reference/publications">Publish, schedule, and read live content.</Card>
  <Card title="Comments" href="/docs/reference/comments">Threaded review comments.</Card>
  <Card title="Approvals" href="/docs/reference/approvals">Request and record approvals.</Card>
  <Card title="Redirects" href="/docs/reference/redirects">Create and resolve URL redirects.</Card>
</Cards>

## System namespaces

Feature APIs, each on its own `cms.api.<namespace>`.

<Cards>
  <Card title="Media" href="/docs/reference/media">Assets, folders, uploads, and variants.</Card>
  <Card title="Variables" href="/docs/reference/variables">Reusable `{{key}}` values.</Card>
  <Card title="Templates" href="/docs/reference/templates">Per-property default values.</Card>
  <Card title="Search" href="/docs/reference/search">Full-text search across entities.</Card>
  <Card title="Releases" href="/docs/reference/releases">Publish many entries together.</Card>
  <Card title="Notifications" href="/docs/reference/notifications">The per-user inbox.</Card>
  <Card title="Users" href="/docs/reference/users">The current user and reviewers.</Card>
  <Card title="Admin" href="/docs/reference/admin">Pruning, scheduled jobs, and search reindex.</Card>
</Cards>

<Callout type="info">
  Plugins extend `cms.api` with their own methods (for example `createTranslation` from [i18n](/docs/plugins/i18n) and the `cms.api.abTest.*` methods from [A/B testing](/docs/plugins/ab-test)). Each plugin page documents its methods the same way.
</Callout>
