---
'@createcms/react': patch
---

`Canvas.Overlay` portals an unstyled layer over the canvas. Selection,
hover and field rings follow measured block and field rects via
`useBlockRect` / `useFieldRect`.
